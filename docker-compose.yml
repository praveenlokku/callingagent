version: "3.9"

services:
  voice-assistant:
    build: .
    container_name: voice-assistant
    ports:
      - "8081:8081"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - .:/app:ro
